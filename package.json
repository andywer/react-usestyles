{
  "name": "react-use-styles",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "watch": "lerna run watch",
    "release": "yarn build && lerna publish",
    "sample:serve": "cd samples/app/ && yarn dev"
  },
  "workspaces": [
    "packages/*",
    "samples/*"
  ],
  "devDependencies": {
    "lerna": "^3.4.3",
    "react": "^16.7.0-alpha.0"
  }
}
